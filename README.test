test with `testling -u`

automated testing with chromium:
use --use-fake-ui-for-media-stream and --use-fake-device-for-media-stream
Use --user-data-dir to start a new instance
use --allow-http-screen-capture and --enable-usermedia-screen-capturing (not yet)

To do test:
1. Install browserify: npm install -g browserify

2. Run: browserify test/basic.js > test_bundle.js

3. Open test.html in browser

NOTE: Replace basic.js with audiomonitor.js for another test
